import classNames from 'classnames';
import { Link } from 'react-router-dom';
import './header.scss';

const Header = () => {
  return (
    <>
      <header>로그인</header>
    </>
  );
};

export default Header;

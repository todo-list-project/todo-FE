import classNames from "classnames";
import { Link } from "react-router-dom";
import "./header.scss";

const Header = () => {
  return (
    <>
      <header>
        헤더
      </header>
    </>
  );
};

export default Header;
